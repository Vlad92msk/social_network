'use client'
import { Video as VidComp } from "../../../../(modules)/Main/sections";

export default async function Video(props) {
  // const ferf = await getProjects()

  return (
      // <VidComp />
      <div>122</div>
  )
}
